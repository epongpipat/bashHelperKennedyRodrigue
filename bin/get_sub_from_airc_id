#!/bin/bash

airc_id=$1

code_dir=`dirname ${0}`
root_dir=`${code_dir}/get_root_dir kenrod`

sub=`cat ${root_dir}/incoming/ids_long-format.csv | grep ${airc_id} | awk -F ',' '{print $2}'`

echo ${sub}

